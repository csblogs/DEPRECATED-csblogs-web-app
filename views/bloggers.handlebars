{{#section 'css'}}
<link rel="stylesheet" href="style/css/bloggers.css">
{{/section}}

<!--
{{#each bloggers}}
    <img src="{{this.avatarUrl}}" alt="{{this.firstName}} {{this.lastName}} Display Picture"/>
    <h2>{{this.firstName}} {{this.lastName}}</h2>
    <i><a href="mailto:{{this.emailAddress}}">{{this.emailAddress}}</a></i>
    <p>{{this.bio}}</p>
    <p>Blog Feed: {{this.feedUrl}}</p>
    <p>Blog Site: {{this.blogWebsiteUrl}}</p>
    <p>Website: {{this.websiteUrl}}</p>
    <p><a href="http://github.com/{{this.githubProfile}}">Github/{{this.githubProfile}}</a></p>
    <p><a href="http://twitter.com/{{this.twitterProfile}}">@{{this.twitterProfile}}</a></p>
    <p>Linkedin name: {{this.linkedInProfile}}</p>
{{/each}}
-->
<!--
<svg id="searchSvg">
    <path d="M0 30 L200 30 L192 22 a10,10 0 1 0 -1,1 L192 22" />
</svg>
-->
<ul>
    {{#each bloggers}}
    <li>
        <div class="avatar" style="background-image: url('{{avatarUrl}}');"></div>
        <h2>{{firstName}} {{lastName}}</h2>
        <a href="{{websiteUrl}}">{{urlFormat websiteUrl}}</a>
    </li>
    {{/each}}
    {{#each bloggers}}
    <li>
        <div class="avatar" style="background-image: url('{{avatarUrl}}');"></div>
        <h2>{{firstName}} {{lastName}}</h2>
        <a href="{{websiteUrl}}">{{urlFormat websiteUrl}}</a>
    </li>
    {{/each}}
</ul>
